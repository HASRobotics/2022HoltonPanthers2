#include <kipr/wombat.h>


int main()
{
	//still need to get robot aligned
	mrp(1, 13200); //moves left wheel once robot is aligned
	mrp(2, 13200); //moves right wheel once robot is aligned
	msleep(30);
	bmd(1);
	bmd(2);
	return 0;
}
